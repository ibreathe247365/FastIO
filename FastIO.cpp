
#include <bits/stdc++.h>

using namespace std;

int main() {
    
    // turns off sync with C streams/buffers
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    // use '\n' instead of endl to speed up output

    // code goes here
    // example:
    int a, b;
    cin >> a >> b;
    cout << a + b << '\n';

    return 0;
}
